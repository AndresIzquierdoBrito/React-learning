import { Button } from 'flowbite-react';
const styles = {
  marginLeft: '50rem',
};

export default function MyPage() {
  return (
    <div>
      <Button style={styles}>Click me</Button>
      
    </div>
  );
}
